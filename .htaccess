Options -Indexes
RewriteEngine On
RewriteBase /

# Serve from dist/ if the file exists there
RewriteCond %{DOCUMENT_ROOT}/dist%{REQUEST_URI} -f
RewriteRule ^(.*)$ dist/$1 [L]

# SPA fallback: everything else serves dist/index.html
RewriteRule ^ dist/index.html [L]
